eureka:
  client:
    service-url:
      defaultZone: http://localhost:19001/eureka/

management:
  tracing:
    sampling:
      probability: 1.0
  zipkin:
    tracing:
      endpoint: http://localhost:9411/api/v2/spans
